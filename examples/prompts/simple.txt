The pratial model is unsolvable because of the conflicting constraints in the domain:
"""
domain Simple2
{
  A ::= new (id: Integer).
  B ::= (id: Integer).
  C ::= (id: Integer).
  D ::= (id: Integer).
  E ::= (id: Integer).

  B(x) :- A(x), x > 0.
  C(x) :- A(x), x < 0.
  D(x) :- A(x).

  goodModel :- B(x).
  badModel :- C(x).
  weirdModel :- goodModel, badModel.
  okModel :- weirdModel, D(x).

  conforms okModel.
}

partial model pm2 of Simple2
{
  A(x).
}
"""

The error message is:
"""
Model not solvable. Unsat core terms below.
Conflicts: Simple2.badModel C(x) 
Conflicts: Simple2.goodModel B(x) 
Conflicts: Simple2.okModel Simple2.weirdModel 
"""

The corrected version of the domain can be:
"""
domain Simple2
{
  A ::= new (id: Integer).
  B ::= (id: Integer).
  C ::= (id: Integer).
  D ::= (id: Integer).
  E ::= (id: Integer).

  B(x) :- A(x), x > -2.
  C(x) :- A(x), x < 0.
  D(x) :- A(x).

  goodModel :- B(x).
  badModel :- C(x).
  weirdModel :- goodModel, badModel.
  okModel :- weirdModel, D(x).

  conforms okModel.
}
"""